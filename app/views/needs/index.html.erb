<h1>Надо сделать</h1>

<%= render 'form' %>

<div id='needs'>
    <% @needs.each do |date, need| %>
      <div class='item <%= "completed" if need[:completed] %>' id='<%= date %>'>  
        <div class='info'><%= need[:date] %>: </div>
        <div class='text'><%= need[:text].gsub(/\\n/, "\n") %></div>
        <div class='edit' style="display:none;">
          <textarea><%= need[:text] %></textarea>
        </div>
        <div class='controls'>
          <input type='checkbox' class='complete_doing' <%= 'checked' if need[:completed] %>>
          <%= link_to 'Удалить', "/#{date}", method: :delete, remote: true %>
        </div>
      </div>
    <% end %>
</div>

<footer>
<div id="hints">
  <p>Для изменения текста задания нажмите на него</p>
  <p>Для сохранения изменений нажмите Ctrl+Enter или просто кликните на пустое пространство</p>
  <p>Поставьте галочку, чтобы пометить задание завершённым</p>
</div>
<div id='copyright'>&copy; Дмитрий Краснов, 2013</div>
</footer>